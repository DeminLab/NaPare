'use client';
import { ReactNode } from 'react';
interface RequestStateProps { kind?: 'error' | 'forbidden' | 'offline'; title: string; description: string; onRetry?: () => void; action?: ReactNode; }
export function RequestState({ kind = 'error', title, description, onRetry, action }: RequestStateProps) { const accent = kind === 'forbidden' ? 'bg-amber-500/15 text-amber-300' : kind === 'offline' ? 'bg-slate-800 text-slate-300' : 'bg-red-500/15 text-red-300'; const symbol = kind === 'forbidden' ? '!' : kind === 'offline' ? '↗' : '×'; return <div className="flex min-h-[240px] flex-col items-center justify-center px-5 py-12 text-center"><div className={`flex h-12 w-12 items-center justify-center rounded-2xl text-lg font-bold ${accent}`}>{symbol}</div><h3 className="mt-4 text-lg font-semibold text-slate-100">{title}</h3><p className="mt-2 max-w-md text-sm leading-6 text-slate-400">{description}</p><div className="mt-6 flex flex-wrap justify-center gap-3">{onRetry && <button onClick={onRetry} className="rounded-xl bg-cyan-600 px-4 py-2.5 text-sm font-semibold text-white transition-colors hover:bg-cyan-500">Повторить</button>}{action}</div></div>; }
