# Production TLS files

Before starting the production stack, mount valid certificate files here:

- `fullchain.pem`
- `privkey.pem`

Do not commit these files. The certificate must cover `napare.ru`, `www.napare.ru`, `staff.napare.ru`, `admin.napare.ru`, and `dev.napare.ru`.
