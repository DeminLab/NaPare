# Source card: `apps/backend/src/auth/strategies/jwt.strategy.spec.ts`

## Canonical source

`apps/backend/src/auth/strategies/jwt.strategy.spec.ts`

## Scope

NestJS backend module, HTTP boundary, authorization or data-service behavior.

## Read sequence

1. Start with [AI context](../../../AI_CONTEXT.md) and the relevant role or backend map.
2. Read this canonical source file.
3. Trace direct imports and its nearest caller before widening the investigation.

## Change boundary

Treat assertions as regression coverage; update only with an intentional behavior change.

## Verification

```powershell
pnpm --filter @napare/backend exec tsc --noEmit
```

